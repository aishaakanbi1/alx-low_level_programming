#include <stdio.h>

/** 
* main - program execution starts here
* Description: this is the main function
* Return: an integer to signify sucess or fail
*/
int main(void)
{ 
         int digit = 0;

         while (digit < 10)
         {
                printf("%d", digit);
                digit++;
         }
         putchar(n);
        
         return (0);
}
